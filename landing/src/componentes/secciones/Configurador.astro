---
// Fuente: Main.dc.html:933-1157. La lógica de selección, precio y marco()
// vive en scripts/configurador.ts + scripts/marco.ts; los textos que dependen
// de lo elegido salen de datos/equipo.ts, que es lo que permite servir el
// primer estado ya armado en vez de esperar al JS.
// El dibujo del equipo sale de componentes/equipo/, compartido con el hero.
// Los id mod-* son los que toca configurador.ts para prender cada módulo.
import Icono from '../equipo/Icono.astro'
import Cuerpo from '../equipo/Cuerpo.astro'
import Gateway from '../equipo/Gateway.astro'
import Solar from '../equipo/modulos/Solar.astro'
import Lora from '../equipo/modulos/Lora.astro'
import Bateria from '../equipo/modulos/Bateria.astro'
import Presion from '../equipo/modulos/Presion.astro'
import Uv from '../equipo/modulos/Uv.astro'
import Temperatura from '../equipo/modulos/Temperatura.astro'
import Humedad from '../equipo/modulos/Humedad.astro'
import Co2 from '../equipo/modulos/Co2.astro'
import Suelo from '../equipo/modulos/Suelo.astro'
import { BASE_USD, PRECIOS } from '../../datos/precios'
import { avisoDe, mensajeDePedido, resumenDe, rotuloDe, seleccionInicial, totalDe } from '../../datos/equipo'
import { linkWhatsApp } from '../../datos/contacto'
import { dinero } from '../../utiles/formato'

const sel = seleccionInicial()
const aviso = avisoDe(sel)
---

<div class="band-alt">
  <section id="equipo" class="build stage">
    <div class="build-head">
      <h2 class="say">Armá el equipo<br>que necesitás.</h2>
    </div>

    <div class="build-grid">
      <div id="rail-sensores" class="rail rail-sensores">
        <div class="rail-h"><span>Sensores</span></div>
        <div class="chips">
          <button type="button" id="chip-temp" class:list={['chip', { on: sel.temp }]} aria-pressed={sel.temp} data-modulo="temp">
            <Icono tipo="temp" clase="ic" />
            <span class="txt"><span class="nm">Temperatura</span><span class="un">°C</span></span>
            <span class="pr">GRATIS</span>
          </button>
          <button type="button" id="chip-hum" class:list={['chip', { on: sel.hum }]} aria-pressed={sel.hum} data-modulo="hum">
            <Icono tipo="hum" clase="ic" />
            <span class="txt"><span class="nm">Humedad</span><span class="un">% HR</span></span>
            <span class="pr">GRATIS</span>
          </button>
          <button type="button" id="chip-co2" class="chip" aria-pressed="false" data-modulo="co2">
            <Icono tipo="co2" clase="ic" />
            <span class="txt"><span class="nm">CO₂</span><span class="un">ppm</span></span>
            <span class="pr">{dinero(PRECIOS.co2)}</span>
          </button>
          <button type="button" id="chip-suelo" class="chip" aria-pressed="false" data-modulo="suelo">
            <Icono tipo="suelo" clase="ic" />
            <span class="txt"><span class="nm">Humedad de suelo</span><span class="un">% vol</span></span>
            <span class="pr">{dinero(PRECIOS.suelo)}</span>
          </button>
          <button type="button" id="chip-uv" class="chip" aria-pressed="false" data-modulo="uv">
            <Icono tipo="uv" clase="ic" />
            <span class="txt"><span class="nm">Radiación UV</span><span class="un">índice</span></span>
            <span class="pr">{dinero(PRECIOS.uv)}</span>
          </button>
          <button type="button" id="chip-pres" class="chip" aria-pressed="false" data-modulo="pres">
            <Icono tipo="pres" clase="ic" />
            <span class="txt"><span class="nm">Presión</span><span class="un">hPa</span></span>
            <span class="pr">{dinero(PRECIOS.pres)}</span>
          </button>
          <a href={linkWhatsApp('Hola, necesito medir algo que no está en el configurador: ')} class="otro-sensor" target="_blank" rel="noopener">
            <span class="nm">¿Otro sensor?</span>
            <span class="dt">Contanos qué necesitás medir y lo charlamos.</span>
          </a>
        </div>
      </div>

      <div class="build-stage">
        <div class="stage-frame">
          <div class="stage-top">
            <span id="equipo-id" class="id">{rotuloDe(sel)}</span>
          </div>

          <svg id="equipo-svg" class="device" viewBox="0 0 780 600" fill="none" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Tu equipo con los módulos elegidos">
            <g id="equipo-outer">
              <g class="unit-main">
                <Solar id="mod-solar" />
                <Lora id="mod-lora" />
                <Bateria id="mod-bat" />
                <Presion id="mod-pres" />
                <Uv id="mod-uv" />
                <Cuerpo />
                <Temperatura id="mod-temp" activo={sel.temp} />
                <Humedad id="mod-hum" activo={sel.hum} />
                <Co2 id="mod-co2" />
                <Suelo id="mod-suelo" />
              </g>
              <Gateway />
            </g>
          </svg>
        </div>

        <p id="aviso-enlace" class="aviso" role="status" hidden={!aviso}>{aviso}</p>

        <div class="segm" role="group" aria-label="Qué módulos ver">
          <button type="button" id="seg-sensores" class="on" aria-pressed="true">
            <span>Sensores</span><span id="cuenta-sensores" class="cuenta"></span>
          </button>
          <button type="button" id="seg-energia" aria-pressed="false">
            <span>Energía y enlace</span><span id="cuenta-energia" class="cuenta"></span>
          </button>
        </div>
      </div>

      <div id="rail-energia" class="rail rail-energia oculto">
        <div class="rail-h"><span>Energía y enlace</span></div>
        <div class="chips">
          <button type="button" id="chip-bat" class="chip" aria-pressed="false" data-modulo="bat">
            <Icono tipo="bat" clase="ic" />
            <span class="txt"><span class="nm">Batería</span><span class="un">autonomía</span></span>
            <span class="pr">{dinero(PRECIOS.bat)}</span>
          </button>
          <button type="button" id="chip-solar" class="chip" aria-pressed="false" data-modulo="solar">
            <Icono tipo="solar" clase="ic" />
            <span class="txt"><span class="nm">Panel solar</span><span class="un">sin red eléctrica</span></span>
            <span class="pr">{dinero(PRECIOS.solar)}</span>
          </button>
          <button type="button" id="chip-lora" class="chip" aria-pressed="false" data-modulo="lora">
            <Icono tipo="lora" clase="ic" />
            <span class="txt"><span class="nm">LoRa</span><span class="un">radio de largo alcance</span></span>
            <span class="pr">{dinero(PRECIOS.lora)}</span>
          </button>
          <button type="button" id="chip-gw" class="chip" aria-pressed="false" data-modulo="gw">
            <Icono tipo="gw" clase="ic" />
            <span class="txt"><span class="nm">Gateway LoRa</span><span class="un">puente a internet</span></span>
            <span class="pr">{dinero(PRECIOS.gw)}</span>
          </button>
        </div>
      </div>
    </div>

    <div id="sumbar" class="sumbar">
      <span class="lbl">Tu equipo</span>
      <span id="resumen-texto" class="list">{resumenDe(sel)}</span>
      <span class="price">
        <span id="delta" class="delta"></span>
        <span id="total" class="n">{dinero(totalDe(sel))}</span>
      </span>
      <a id="pedir" href={linkWhatsApp(mensajeDePedido(sel))} class="cta cta-solid" target="_blank" rel="noopener">Pedir este equipo</a>
    </div>

    <p class="nota-precio">Equipo base y Wi-Fi vienen en todos los equipos: {dinero(BASE_USD)}.</p>
    <p class="nota-precio">Al elegir LoRa entra también el gateway, que es un segundo equipo: sacalo si ya tenés uno instalado.</p>
  </section>
</div>

<script>
  import '../../scripts/configurador'
  import '../../scripts/acomodar'
</script>
